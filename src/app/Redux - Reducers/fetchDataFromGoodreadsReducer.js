
import * as types from '../Redux - Action Creators/actionTypes';

export function fetchDataFromAPI(state = '', action) {
    switch (action.type) {
        case types.FETCH_DATA_FROM_API:
            
        default:
            return state;
    }
}